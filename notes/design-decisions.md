# Design Decisions

Why a given tool is single-language, why a bash/python pairing was dropped
or added, or any repo-level architectural call that would otherwise get
lost in a commit message. Add an entry per decision, dated.

## Log

- (repo scaffolded) - dual-implementation model chosen over two separate
  repos. See README.md for the reasoning. Per-tool language calls captured
  in ROADMAP.md at scaffold time; any tool where the actual build deviates
  from the planned language split gets an entry here explaining why.
